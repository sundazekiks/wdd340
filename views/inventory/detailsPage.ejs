<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { res.redirect('/') } %>

        <%# messages() %>

            <%- container %>
                <a href="/account/amortization/<%= v_id %>" id="amortize">Amortize</a>